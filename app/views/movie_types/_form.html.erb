<%= form_for(@movie_type) do |f| %>
  <% if @movie_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@movie_type.errors.count, "error") %> prohibited this movie_type from being saved:</h2>

      <ul>
      <% @movie_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
