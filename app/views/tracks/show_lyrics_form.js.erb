var $ = jQuery;

var tabContent = "<%= escape_javascript(render partial: 'lyrics_form',
                                                locals: {track: 'track',
                                                         genres: 'genres'}) %>";

$('#tab-area').html(tabContent);
