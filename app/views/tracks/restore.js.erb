var $ = jQuery

var content = "<%= escape_javascript(render(partial: "track",
                                            object: @track)) %>";

var $track = $("#track-<%= @track.id %>");
$track.replaceWith(content);

var notice = "<%= escape_javascript(flash[:notice]) %>";
$('#notice').html(notice);