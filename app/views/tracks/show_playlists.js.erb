var $ = jQuery;

$('#tab-area').html("<%= escape_javascript(render partial: 'playlists',
                                                  locals: {track: 'track',
                                                           genres: 'genres'}) %>");
