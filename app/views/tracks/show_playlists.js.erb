var $ = jQuery;

var tabContent = "<%= escape_javascript(render partial: 'playlists',
                                               locals: {track: 'track',
                                                        genres: 'genres'}) %>"

$('#tab-area').html(tabContent);
