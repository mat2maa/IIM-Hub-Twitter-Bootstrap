var $ = jQuery

alert("hello");

var content = "<%= escape_javascript(render 'video', object: @video) %>";
alert(content);

var $video = $("#video-<%= @video.id %>");

$video.html(content);

var notice = "<%= escape_javascript(flash[:notice]) %>";

$('#notice').html(notice);