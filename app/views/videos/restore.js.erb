var $ = jQuery

var content = "<%= escape_javascript(render(partial: "video",
                                            object: @video)) %>";

var $video = $("#video-<%= @video.id %>");
$video.replaceWith(content);

var notice = "<%= escape_javascript(flash[:notice]) %>";
$('#notice').html(notice);