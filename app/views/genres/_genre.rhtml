  <tr id="genre-row-<%=h genre.id %>">
    <td class="list-item"><%=h genre.id %></td>
    <td class="list-item"><%=h genre.name %></td>
    <td class="list-item"><%= link_to 'Edit', edit_genre_path(genre) %></td>
    <td class="list-item"><%= link_to 'Delete', genre, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>