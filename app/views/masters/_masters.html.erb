<% if @search.count > 0 %>
	<%= @search.count %> masters found
	<table cellspacing="0" width="100%">
	<tr>
		<th><%= remote_order_by_link  :id %></th>
		<th>Programme Title</th>
		<th><%= remote_order_by_link  :episode_title %></th>
		<th><%= remote_order_by_link  :episode_number %></th>
		<th><%= remote_order_by_link  :location %></th>
		<th colspan="2"></th>			
	</tr>
	
	<%= render :partial => "master", :collection => @masters %>
	</table>
	
  <% if @search.page_count > 1 %>
		<br/><br/>
		<div class="page_links">
			<%=	page_links()%>
		</div>
  <% end %>
<% else %>
	
	No masters were found

<% end %>