var $ = jQuery;

var newRow = " <%= escape_javascript(render(partial: "airline", object: @airline)) %> ";

$("#airline_form")
        .find("input[type=text]").val("");

$("#airlines>tbody").append(newRow);
