<%= render partial: 'header' %>
<div id="content">
  <center>
    <%= render partial: 'form' %>
    <br/>
    <table border="0" cellpadding="10" cellspacing="0" width="100%" id="master_languages">
      <tr>
        <td class="list-header">ID</td>
        <td class="list-header">Language</td>
        <td colspan="2" class="list-header">&nbsp;</td>
      </tr>
      <%= render partial: 'master_language',
                 collection: @master_languages %>
    </table>
    <br/>
    <br/>
    <%= will_paginate @master_languages %>
    <%
      def items_per_page_select(collection = MasterLanguages)
        select_tag :per_page, options_for_select([10, 20, 50, 100], collection.per_page)
      end
    %>
    <%= form_for '/master_languages', method: :get, html: { class: 'items_per_page_select' } do |f| %>
      <span>Items per page: </span>
      <%= items_per_page_select @master_languages %>
      <%= f.submit "Submit" %>
    <% end %>
  </center>
</div>