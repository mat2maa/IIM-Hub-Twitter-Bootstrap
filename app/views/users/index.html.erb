<%= render :partial => 'header' %>
<div id="content">
	<center>
<h2>Users</h2>
<% form_for (:search, :url => users_path) do |f| %>
    <%= text_field_tag :keywords %><%= f.submit "Search" %>
<% end %>

<% if @users_count > 0 %>
    <div id="admin-actions"><%= @users_count %> entries found</div>
<table border="0" cellpadding="10" cellspacing="0" width="80%" class="list">
  <tr >
    <th><%= order_by_link :id %></th>
		<th><%= order_by_link :login %></th>
		<th>Last Login</th>
		<th>Last Request Date</th>
		<th colspan="2">&nbsp;</th>
  </tr>
	<%= render :partial => 'user', :collection => @users %>
</table>
<div id="admin-actions">
	Per page: <%= per_page_select %>
  <% if @search.page_count > 1 %>
     <br />Page: <%= page_select %>
  	<% end %>
	<% else %>
    No entries were returned
	<% end %>
	</center>
</div>
</div>