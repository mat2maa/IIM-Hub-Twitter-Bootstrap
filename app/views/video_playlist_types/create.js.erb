var $ = jQuery;

var newRow = " <%= escape_javascript(render(partial: "video_playlist_type", object: @video_playlist_type)) %> ";

$("#video_playlist_type_form")
        .find("input[type=text]").val("");

$("#video_playlist_types>tbody").append(newRow);
