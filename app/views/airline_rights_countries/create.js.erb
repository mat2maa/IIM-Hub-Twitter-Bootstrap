var $ = jQuery;

var newRow = " <%= escape_javascript(render(partial: "airline_rights_country",
object: @airline_rights_country)) %> ";

$("#airline_rights_country_form")
        .find("input[type=text]").val("");

$("#airline_rights_countrys>tbody").append(newRow);
