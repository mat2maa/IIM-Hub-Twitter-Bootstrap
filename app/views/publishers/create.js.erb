var $ = jQuery;

var newRow = " <%= escape_javascript(render(partial: "publisher",
object: @publisher)) %> ";

$("#publisher_form")
        .find("input[type=text]").val("");

$("#publishers>tbody").append(newRow);
