require 'spec_helper'

describe ScreenerPlaylist do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    ScreenerPlaylist.create!(@valid_attributes)
  end
end
