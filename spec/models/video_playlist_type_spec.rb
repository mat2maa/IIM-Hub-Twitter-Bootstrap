require 'spec_helper'

describe VideoPlaylistType do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    VideoPlaylistType.create!(@valid_attributes)
  end
end
